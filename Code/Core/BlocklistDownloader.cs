using System;

namespace BlocklistUpdater.Core
{
    class BlocklistDownloader : IBlocklistDownloader
    {
        public TempFile Update(Blocklist blocklist)
        {
            throw new NotImplementedException();
        }
    }
}